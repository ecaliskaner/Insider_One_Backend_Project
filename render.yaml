services:
  - type: web
    name: insider-football-simulation
    env: docker
    dockerfilePath: Dockerfile
    autoDeploy: true
    branch: main
    healthCheckPath: /readyz
    envVars:
      - key: PORT
        value: 8080
      - key: DB_PATH
        value: /data/league.db
      - key: SIM_SEED
        value: 42
      - key: WEATHER_PROVIDER
        value: local
      - key: TEAM_STRENGTH_PROVIDER
        value: local
    disk:
      name: sqlite-data
      mountPath: /data
      sizeGB: 1
